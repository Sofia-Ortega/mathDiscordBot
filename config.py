# MUST DEFINE: token, channel_id, bot_prefix
# OPTIONAL: category

CONFIG = {     
    'token': '', # add your token here
    'channel_id': , # main/general channel (This MUST be an INT, not a string)
    'category': '', # if you want the game channels to be created in a particular category
    'bot_prefix': '!' # command prefix (i.e. !start or -start)
}
